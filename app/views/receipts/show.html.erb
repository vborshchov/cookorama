<% content_for :add_to_favourite do%>
  <!-- <#%= link_to raw("<i class='fi-heart #{@is_favorite.to_s}></i>"), receipts_toggle_favorite_path(link: params[:link]), remote: "true"%> -->
  <%= link_to raw("<i class='fi-heart #{@is_favorite}'></i>"), receipts_toggle_favorite_path(link: params[:link]), remote: "true"%>
<% end %>

<% content_for :right_menu do%>
  <li><label>Інгредієнти</label></li>
  <%= render inline: @ingredients %>
<% end %>

<h2><%= @receipt.title %></h2>
<%= render inline: @receipt.top_tags %>
<%= render inline: @receipt.img %> <br>
<%= render inline: @receipt.tags %>
<h5>Інгредієнти</h5>
<%= render inline: @receipt.ingredients %>
<%= render inline: @receipt.content %>


<div class="row">
  <div class="comments columns small-12 middle-11 large-10"  id="comments">
    <%= render inline: @comments.to_html.html_safe %>
  </div>
</div>